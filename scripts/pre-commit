#!/bin/sh
# Récupère les fichiers Ocaml ajoutés ou modifiés dans l'index
FILES=$(git diff --cached --name-only --diff-filter=ACM | grep -E '\.(ml|mli)$')

# S'il y a aucun fichier Ocaml modifier on quitte
if [ -z "$FILES" ]; then
  exit 0
fi

echo "🔍 Vérification du formatage OCaml..."
# Vérifie que ocaml format est installé
if ! command -v ocamlformat >/dev/null 2>&1; then
  echo "❌ ocaml format n'est pas installé."
  echo "Installe-le avec : opam install ocaml format"
  exit 1
fi

# Formate les fichiers
for file in $FILES; do
  echo "→ Formatage de $file"
  ocamlformat --inplace "$file"
  git add "$file"
done

echo "✅ Formatage terminé."
exit 0